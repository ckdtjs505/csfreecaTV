header.header
    .header__column
        a(href=routes.home)
            img(src="/img/rogo.png", alt="", srcset="")
        form(action=routes.search, method="get")
            input(type="text", name="search_query")
            button
                i.fas.fa-search
    .header__column
        if !loggedUser
            ul
                li
                    a(href=`/videos${routes.upload}`)
                        img(src="/img/up-arrow.png", alt="", srcset="")
                li
                    a(href="#") 
                        img(src="/img/menu.png", alt="", srcset="")
                li
                    a(href=routes.login) 로그인 
        else 
            ul
                li
                    a(href=`/videos${routes.upload}`)
                        img(src="/img/up-arrow.png", alt="", srcset="")
                li
                    a(href=`/users${routes.me}`) #{loggedUser.name}
                li
                    a(href=routes.logout) logout
