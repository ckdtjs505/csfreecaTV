extends layout/main
include mixins/videoBlock

block content
    .video 
        each video in videos    
            +videoBlock({
                title: video.title,
                views: video.views,
                videofile: video.videofile
            })